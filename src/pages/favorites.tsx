import React, {Component} from 'react';
import PropTypes, { array, string, number } from 'prop-types';
import './App.css';

class Favorites extends Component{

    render(){

        let listOfFavoritesPokemons = [];

        
        return(
            'work in progress'
        )
    }
}

export default Favorites